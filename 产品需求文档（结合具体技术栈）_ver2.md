# JourneyCraft 智能旅游推荐系统 - 最终产品需求文档 (PRD)

---

## 1. 文档概述

### 产品背景

JourneyCraft 是一款面向校园与景区场景的智能旅游推荐系统，为用户提供从行前推荐规划、行中导航游览、行后记录分享的全流程旅游服务。本产品同时服务于数据结构课程设计要求，系统核心算法需基于自设计数据结构实现。

### 目标用户

- **主要用户**：大学生群体，具有校园探索与周边旅游需求
- **次要用户**：景区游客，需要景区内导航、景点推荐等服务
- **用户规模**：系统设计支持不少于 10 名注册用户

### 核心目标

> 为用户提供"推荐 → 规划 → 导航 → 记录"的一站式旅游体验闭环，以图论与排序算法为核心驱动，基于 Vue 3 + Spring Boot + MySQL/MongoDB 混合存储架构，构建真实可用的 Web 应用系统。

### 适用范围

本 PRD 覆盖 JourneyCraft V2.0 完整功能范围。P0/P1 的划分用于**指导开发优先级与架构分层**。课程要求的所有功能均需在 8 周项目周期内交付。

---

## 2. 技术架构

### 2.1 技术栈总览

| 层级 | 技术选型 | 版本 | 选型理由 |
|:---|:---|:---|:---|
| **前端框架** | Vue 3 + Vite | 3.4 / 5.x | 轻量快速，Composition API 利于复杂交互逻辑组织 |
| **UI 组件库** | Element Plus | 2.x | 企业级组件库，表格/表单/弹窗等基础组件完备 |
| **前端状态管理** | Pinia | 2.x | 轻量、TypeScript 友好，替代 Vuex |
| **地图引擎** | 高德地图 JS API | 2.0 | 国内地图服务成熟，支持路径规划展示、标记覆盖物、图层控制 |
| **HTTP 客户端** | Axios | — | 请求/响应拦截器、自动 JSON 转换 |
| **前端路由** | Vue Router | 4.x | 支持 4 个一级页面的路由组织 |
| **后端框架** | Spring Boot | 3.2.x | Java 生态成熟，MyBatis-Plus 简化 CRUD |
| **ORM** | MyBatis-Plus | 3.5.x | 代码生成、分页插件、条件构造器，减少样板代码 |
| **API 文档** | Knife4j | 4.4.x | Swagger UI 增强，前后端接口联调便利 |
| **DTO 映射** | MapStruct | 1.5.x | 编译期自动映射 Entity ↔ DTO，零反射开销 |
| **工具类** | Hutool | 5.8.x | 字符串/集合/日期/加密等通用工具 |
| **JSON 序列化** | Jackson | — | Spring Boot 默认序列化方案 |
| **认证方案** | Spring Security + JWT | — | 无状态 Token 认证，适合前后端分离 |
| **关系型数据库** | MySQL | 8.0 | 存储：用户、景点、建筑、设施、道路图、行程等结构化核心数据 |
| **文档型数据库** | MongoDB | 7.0 | 存储：旅游日记（富文本+媒体元数据）、评论等非结构化/半结构化数据 |
| **对象存储** | MinIO | — | 存储用户上传的图片、视频等文件资源 |

### 2.2 存储分层策略

```
┌─────────────────────────────────────────────────┐
│                    前端 (Vue 3)                   │
│   主页  │  行程  │  日记  │  个人中心               │
├─────────────────────────────────────────────────┤
│                 后端 (Spring Boot)                 │
│   Controller → Service → Algorithm → Repository  │
├──────────────┬──────────────┬────────────────────┤
│   MySQL 8.0  │  MongoDB 7.0 │     MinIO          │
│ 结构化核心数据 │ 日记/评论/富文本 │ 图片/视频文件     │
└──────────────┴──────────────┴────────────────────┘
```

**MySQL 承载（结构化核心数据）：**

| 数据域 | 典型实体 |
|--------|---------|
| 用户域 | 用户表、用户偏好表、收藏表、历史记录表 |
| 景点域 | 景点表、景点分类表、建筑表、服务设施表 |
| 图数据域 | 节点表（图顶点）、边表（图边+权重）、设施类型枚举 |
| 行程域 | 行程表、行程节点表 |
| 算法域 | 路径规划结果缓存表 |

**MongoDB 承载（非结构化/半结构化数据）：**

| 数据域 | 典型集合 |
|--------|---------|
| 日记域 | diary 集合（标题、富文本内容、媒体引用列表、标签、评分、路径引用、content_type） |
| 评论域 | comment 集合（日记评论、景点点评） |
| 日记互动域 | diary_interaction 集合（浏览记录、点赞记录） |

**MinIO 承载（文件资源）：**
- 用户上传的图片、视频
- 日记附件
- 旅游动画生成产物

### 2.3 扩展性设计原则

本系统采用**模块化分层架构**，确保未来功能可平滑接入：

| 设计策略 | 实施方式 | 技术栈落地点 | 覆盖的未来功能 |
|---------|---------|------------|--------------|
| **策略模式 (Strategy Pattern)** | 路径规划引擎支持可插拔的路径评估策略，通过统一接口注入不同权重计算逻辑 | Spring Boot Service 层策略接口 + Spring 依赖注入 | 多交通工具策略、拥挤度感知导航、反向游览建议 |
| **推荐引擎抽象** | 推荐模块基于评分函数抽象，输入为用户画像向量，输出为排序结果，支持单用户/多用户聚合两种模式 | 独立 RecommendationService，依赖 UserRepository 获取用户画像 | 同行人协同规划、兴趣匹配推荐 |
| **图模型分层** | 导航图统一采用 `G = (V, E, W)` 加权图模型，存储于 MySQL 节点表/边表；MVP 使用二维坐标，室内导航通过节点增加 `floor` 属性扩展为三维图 | MySQL 节点表预留 `building_id`、`floor`、`room` 可空字段 | 室内导航（楼内导航、跨楼层导航） |
| **内容存储分离** | 日记系统将文本内容、媒体附件、元数据分离：MongoDB 存富文本+元数据，MinIO 存媒体文件，MySQL 存日记摘要索引 | MongoDB diary 集合 + MinIO 文件桶 + MySQL diary_index 表 | 自动生成日记、旅游动画生成、全文检索 |
| **用户偏好模型化** | 用户表独立存储兴趣偏好（景点类别、菜系、出行方式等），采用 JSON 结构化字段存储 | MySQL user_preference 表，preference_data JSON 列 | 同行人协同规划、个性化推荐优化 |
| **外部服务适配层** | 旅游服务链接（购票、导航、美食平台）通过配置化适配器对接 | Spring Boot `@Service` 适配器 + 配置文件注册 | 更多外部服务接入 |

### 2.4 技术债务规避

| 风险点 | 预防措施 | 技术栈落地 |
|--------|---------|----------|
| 路径规划引擎无法支持拥挤度权重 | 边权重设计为 `W = α·距离 + β·时间 + γ·拥挤度` 加权模型，MVP 阶段 γ=0 | MySQL edge 表存储 `weight_distance`、`weight_time`、`weight_congestion` 三个权重列 |
| 室内导航需重建图结构 | 统一图模型中节点包含 `building_id`、`floor`、`room` 可空属性，室内节点是室外节点的自然扩展 | MySQL node 表字段预留，MVP 阶段室内节点不录入 |
| 同行人协同推荐需改造推荐模块 | 推荐引擎评分函数支持接收 `List<UserProfile>`，MVP 阶段传入单元素列表 | `RecommendationService.score(UserProfile... profiles)` 可变参数设计 |
| 日记全文检索性能瓶颈 | MySQL 存日记摘要+标题索引，MongoDB 原生支持全文索引 | MongoDB diary 集合创建 text index，MVP 阶段先启用 |
| 多交通工具组合导致算法复杂度失控 | 交通工具作为边的属性标签，对应不同权重系数表 | MySQL edge 表增加 `transport_type` 枚举列 + transport_weight 配置表 |
| 推荐结果排序性能（Top-K） | 采用自实现最小堆进行 Top-K 提取 | Java 自实现 `MinHeap<T>` 泛型类，不依赖第三方库 |
| 双数据库（MySQL + MongoDB）事务一致性 | 对跨库操作采用最终一致性：先写 MySQL 保证核心数据一致，再异步写 MongoDB | Spring `@Transactional` 保证 MySQL 事务，MongoDB 写入通过事件/异步任务 |

---

## 3. 功能需求列表（MVP 版本 - P0）

> P0 功能构成核心业务闭环，是系统可运行的最小功能集合。

### 3.1 旅游前 - 推荐与查询

| 模块 | 功能点 | 优先级 | 需求描述 | 技术实现备注（技术栈 + 扩展性） |
|:---|:---|:---|:---|:---|
| 推荐系统 | 景点/学校推荐 | P0 | 用户可选择不同景点和学校作为目的地，系统根据旅游热度、评价和个人兴趣进行推荐，支持按热度、评价排序。需实现 Top-K 优化，快速获取前若干热门结果 | 后端：自实现 `MinHeap<Attraction>` 完成排序；前端：Element Plus Table/卡片列表展示；**扩展**：评分函数支持 `List<UserProfile>` 输入，后续接入多人协同仅需修改调用方 |
| 推荐系统 | 美食推荐 | P0 | 在选中景点/学校后，按热度、评价和距离对美食排序；支持按菜系过滤；支持按名称/菜系/饭店名称模糊查询 | 后端：复用推荐排序框架，距离通过 Dijkstra 计算实际路径距离；前端：筛选器组件（Element Plus Select） |
| 信息查询 | 景点信息查询 | P0 | 用户可通过名称、类别、关键字搜索景点/学校；多结果时支持按热度、评价排序；展示景点介绍、点评信息、美食信息 | 后端：MyBatis-Plus 条件构造器动态拼接查询；MongoDB 存储点评富文本；前端：搜索框 + 结果列表 + 详情抽屉 |
| 信息查询 | 场所查询 | P0 | 用户选中某景点/场所后，查询附近一定范围内的服务设施（超市、卫生间、餐饮等），按实际距离（非直线距离）排序，支持按类别过滤 | 后端：基于 Dijkstra 图搜索计算真实距离；MySQL facility_type 枚举字段便于扩展设施种类 |
| 用户系统 | 用户注册/登录 | P0 | 用户可通过账号密码注册登录，支持用户信息管理 | 后端：Spring Security + JWT 无状态认证；密码 Hutool BCrypt 加密；前端：Axios 拦截器自动附加 Token |
| 用户系统 | 个人偏好设置 | P0 | 用户可设置兴趣偏好（景点类别、菜系偏好等）和出行偏好（步行/骑行等） | 后端：MySQL user_preference 表，`preference_data` JSON 列存储结构化偏好；前端：Element Plus 表单组件 |

### 3.2 旅游中 - 导航与规划

| 模块 | 功能点 | 优先级 | 需求描述 | 技术实现备注（技术栈 + 扩展性） |
|:---|:---|:---|:---|:---|
| 路径规划 | 景区内导航（单目标） | P0 | 用户输入目标景点，系统从当前位置规划最优路径；支持地图展示和路径展示 | 后端：自实现 Dijkstra/A* 算法，操作内存中的加权图结构；前端：高德地图 JS API 绘制 Polyline 路径线 |
| 路径规划 | 多目标路线规划 | P0 | 用户输入多个目标点，系统规划从起点依次游览全部目标并返回起点的最优路线（TSP 问题） | 后端：自实现贪心 + 2-opt 近似算法；**扩展**：预留策略接口，后续可替换为模拟退火等更优近似算法 |
| 路径规划 | 多种路径规划策略 | P0 | 支持最短距离策略（总路程最短）、最短时间策略（结合道路拥挤度后总时间最短） | 后端：策略模式，`PathStrategy` 接口 + `DistanceStrategy` / `TimeStrategy` 实现；**扩展**：后续增加 `CongestionAwareStrategy` 实现 |
| 路径规划 | 多交通工具策略 | P0 | 校园场景：步行、自行车；景区场景：步行、电瓶车；不同工具可组合以实现时间最优 | 后端：MySQL edge 表 `transport_type` 列 + transport_weight 配置表，算法层权重查表；前端：Element Plus Radio Group 切换 |
| 路径规划 | 地图展示 | P0 | 在地图上展示景区/校园道路图、当前位置、目标点、规划路径 | 前端：高德地图 JS API，Marker 标注景点，Polyline 绘制路径，自定义图层叠加道路网络；**扩展**：预留图层控制接口，后续叠加拥挤度热力图层 |
| 行程管理 | 保存/查看行程 | P0 | 用户可将规划好的行程保存，支持查看已保存行程和历史路径回顾 | 后端：MySQL itinerary 表 + itinerary_node 表（路径节点序列 + 时间戳）；**扩展**：行程节点序列为后续日记自动生成提供数据源 |
| 服务链接 | 旅游服务链接 | P0 | 针对选定景点，提供购票链接、导航出行链接、美食服务链接等外部服务入口 | 后端：MySQL service_link 表 + Spring Boot `@Service` 适配器模式；前端：景点详情页外链按钮组 |

### 3.3 旅游后 - 记录与分享

| 模块 | 功能点 | 优先级 | 需求描述 | 技术实现备注（技术栈 + 扩展性） |
|:---|:---|:---|:---|:---|
| 日记系统 | 旅游日记 CRUD | P0 | 用户可撰写旅游日记（支持文字、图片、视频），可编辑、删除已发布日记 | 后端：MongoDB diary 集合存储富文本+元数据，MinIO 存媒体文件，MySQL diary_index 表存摘要索引；前端：富文本编辑器 + 文件上传组件；**扩展**：`content_type` 字段区分手动/自动生成 |
| 日记系统 | 日记浏览与评分 | P0 | 用户可浏览所有日记，按热度（浏览量）、评分排序；可对日记进行评分 | 后端：MongoDB diary 集合 + diary_interaction 集合；MapStruct 映射 DiaryDocument → DiaryVO；前端：瀑布流/卡片列表 + 评分组件 |
| 日记系统 | 日记检索 | P0 | 支持按目的地查询日记；支持按热度、评分排序；支持按日记名称精确查询 | 后端：MongoDB text index 原生全文检索 + MySQL diary_index 复合索引辅助；**扩展**：后续全文检索直接启用 MongoDB 原生能力，无需切换引擎 |
| 个人中心 | 我的收藏 | P0 | 用户可收藏景点、学校、美食、日记、路线 | 后端：MySQL favorite 表采用多态设计（`target_type` 枚举 + `target_id`），统一一张表管理所有收藏类型 |
| 个人中心 | 历史记录 | P0 | 用户可查看搜索历史、浏览历史、路径回顾、场所查询历史 | 后端：MySQL user_history 表，统一审计日志结构；前端：时间线组件展示 |

---

## 4. 功能需求列表（未来迭代规划 - P1）

> P1 功能为体验增强型或技术复杂度较高的功能，依赖 P0 阶段奠定的架构基础。所有 P1 功能均需在课程周期内交付。

### 4.1 协同与智能化增强

| 模块 | 功能点 | 预计阶段 | 需求描述 | 依赖的 MVP 基础（继承关系） |
|:---|:---|:---|:---|:---|
| 推荐系统 | 同行人协同规划 | P1 | 多人出行时，综合每位同行人的偏好进行折中推荐，输出多人最优方案 | 继承 `RecommendationService.score(List<UserProfile>)`，增加多用户偏好聚合层（加权平均）；复用 user_preference 表的偏好模型 |
| 日记系统 | 自动生成旅游日记 | P1 | 根据用户的旅行路径、拍摄照片、文字描述等，自动生成旅游日记 | 继承 diary 集合 `content_type` 字段（新增 `AUTO_GENERATED` 类型）；复用 itinerary_node 路径数据 + MinIO 媒体资源 |
| 日记系统 | 旅游动画生成 | P1 | 基于图片、文字描述等信息生成旅游动画 | 复用 MinIO 已存储的媒体资源；新增 animation_output MinIO 桶存储生成产物 |
| 个人中心 | 消费账单自动生成 | P1 | 根据门票购买记录、酒店预订记录等，自动生成消费账单，按交通/住宿/餐饮/购物分类统计 | 新增 MySQL expense_bill 表 + expense_item 表；复用 itinerary 时间线数据关联消费记录 |

### 4.2 导航与体验增强

| 模块 | 功能点 | 预计阶段 | 需求描述 | 依赖的 MVP 基础（继承关系） |
|:---|:---|:---|:---|:---|
| 导航系统 | 室内导航 | P1 | 支持建筑物内部导航：大门→电梯、楼层间导航、楼层内→房间导航 | 继承 MySQL node 表预留字段（`building_id`、`floor`、`room`），Dijkstra/A* 算法无需修改，仅加载包含室内节点的子图 |
| 导航系统 | 实时拥挤度感知 | P1 | 实时显示景区内不同区域拥挤程度（红/黄/绿标识），检测拥堵时自动提示重新规划路线 | 继承 `PathStrategy` 策略接口，新增 `CongestionAwareStrategy` 实现；复用 edge 表 `weight_congestion` 权重列；前端高德地图叠加热力图层 |
| 导航系统 | 反向游览建议 | P1 | 当检测到拥堵时，引导用户前往人少区域游览 | 依赖拥挤度感知模块；复用 `RecommendationService` 输出替代景点建议 |
| 互动功能 | AR 式打卡引导 | P1 | 用户到达景点后，系统自动弹出推荐拍照角度/点位，简化为"拍照点推荐 + 打卡任务"模式 | 新增 MySQL photo_spot 表（作为 attraction 的子资源）；前端到达景点范围触发弹窗 |
| 日记系统 | 日记全文检索 | P1 | 支持按日记内容进行全文关键词检索 | 直接启用 MongoDB diary 集合的 text index，无需架构变更 |
| 日记系统 | 日记按距离推荐 | P1 | 用户输入目的地查看相关日记时，增加按距离排序 | 复用 Dijkstra 最短路径计算结果作为距离指标，传入日记排序流程 |

---

## 5. 非功能性需求

### 5.1 数据规模要求

| 指标 | 最低要求 | 存储方案 |
|------|---------|---------|
| 景区和校园数量 | ≥ 200 个 | MySQL attraction 表 |
| 每个景区/校园内建筑物数量 | ≥ 20 个 | MySQL building 表 |
| 服务设施种类 | ≥ 10 种 | MySQL facility_type 枚举 |
| 服务设施总数量 | ≥ 50 个 | MySQL facility 表 |
| 道路图节点数 | ≥ 200 个 | MySQL node 表 |
| 道路图边数 | ≥ 200 条 | MySQL edge 表 |
| 系统注册用户数 | ≥ 10 人 | MySQL user 表 |

### 5.2 性能要求

| 场景 | 指标 | 要求 | 优化手段 |
|------|------|------|---------|
| 景点推荐 | Top-K 查询响应 | < 1 秒（K=10） | 自实现 MinHeap，不进行全量排序 |
| 路径规划 | 单目标最短路径 | < 500ms（200+ 节点图） | Dijkstra + 优先队列（自实现） |
| 路径规划 | 多目标 TSP | < 3 秒（≤10 目标点） | 贪心 + 2-opt 近似算法 |
| 场所查询 | 附近设施搜索 | < 1 秒 | 图搜索真实距离，MySQL 索引加速 |
| 日记浏览 | 列表加载 | < 1 秒（分页 20 条） | MyBatis-Plus 分页插件 + MongoDB limit/skip |
| 日记检索 | 全文检索 | < 2 秒 | MongoDB text index |

### 5.3 安全要求

| 项目 | 要求 | 技术实现 |
|------|------|---------|
| 用户认证 | 无状态 Token 认证 | Spring Security + JWT |
| 密码存储 | 禁止明文，BCrypt 加密 | Hutool `BCrypt.hashpw()` |
| 接口鉴权 | 写操作需验证登录态 | Spring Security `@PreAuthorize` / Filter |
| 输入校验 | 参数校验 + SQL 注入防护 | MyBatis-Plus 参数化查询 + Spring Validation `@Valid` |
| 文件上传 | 限制文件类型和大小 | Spring Boot `MultipartResolver` 配置 + 后端校验 |
| 跨域 | 前后端分离跨域处理 | Spring Boot CORS 配置 / Axios 配置 |

### 5.4 课程硬性要求

| 项目 | 要求 |
|------|------|
| 功能完整性 | 所有功能（P0 + P1）均需实现，验收时应能完整演示 |
| 算法自研 | 核心算法（Dijkstra/A*、TSP、Top-K 排序等）需基于自设计数据结构实现，不得直接调用第三方路径规划/推荐库 |
| 贴近真实 | 系统需尽量贴近真实应用场景，最终形成真实可用的软件系统 |
| 数据结构覆盖 | 需体现图（路径规划）、树（分类体系）、排序（推荐排序）、查找（信息查询）等核心数据结构 |

---

## 6. 数据结构映射（课程核心要求）

| 数据结构 | 应用场景 | 对应功能模块 | 存储位置 |
|---------|---------|------------|---------|
| **图 (Graph)** | 景区/校园道路网建模 | 路径规划（Dijkstra/A*）、TSP 多目标规划 | MySQL node + edge 表 → 内存加载为邻接表/邻接矩阵 |
| **排序算法** | 推荐、查询结果的有序输出 | Top-K 堆排序（推荐模块）、多键排序（热度/评分/距离） | Java 自实现排序算法类 |
| **查找算法** | 景点查询、日记检索 | 哈希查找（ID 定位）、二分查找（排序结果定位）、树形索引（分类检索） | MyBatis-Plus 查询 + 自实现查找算法 |
| **树 (Tree)** | 景点分类层级、设施类型层级 | 分类体系的组织与快速过滤 | MySQL 表的自引用外键（parent_id）或前端树形组件 |
| **优先队列/堆** | Top-K 推荐结果提取、Dijkstra 最短路径 | 最小堆实现 Top-K 热门推荐、优先队列加速 Dijkstra | Java 自实现 `MinHeap<T>` + `PriorityQueue<T>` |
| **栈/队列** | 路径回溯、室内导航楼层层级遍历 | DFS/BFS 路径探索、楼层递归导航 | Java 自实现栈/队列结构 |

---

## 7. 风险与应对

| 风险编号 | 风险描述 | 影响范围 | 概率 | 应对预案 |
|:---|:---|:---|:---|:---|
| R1 | TSP 多目标规划在目标点较多时性能急剧下降 | 路径规划模块 | 中 | 采用贪心 + 2-opt 近似算法；限制单次规划目标点上限（10 个） |
| R2 | 室内导航三维图数据构造工作量大 | 导航系统 | 高 | 统一图模型已预留楼层扩展能力；室内数据采用模板化生成 |
| R3 | 同行人协同推荐偏好聚合算法设计复杂 | 推荐系统 | 中 | 采用加权平均聚合，后续可迭代优化 |
| R4 | MySQL + MongoDB 双库事务一致性 | 全局 | 中 | 核心数据（用户/景点/图）全走 MySQL 保证强一致；日记等走 MongoDB，采用最终一致性 |
| R5 | 高德地图 JS API 集成开发工作量大 | 前端 | 中 | 优先实现基础地图展示和路径 Polyline；拥挤度热力图等高级功能放 P1 |
| R6 | 全部功能 8 周交付时间紧张 | 项目整体 | 高 | 严格 P0 → P1 分阶段；P1 功能做合理简化（如 AR 打卡简化为拍照点推荐） |
| R7 | 日记自动生成依赖 AI 能力，课程环境受限 | 日记系统 | 中 | 简化为模板拼接（路径节点 + 时间戳 + 照片），无需 NLP |
| R8 | MinIO 部署配置增加运维复杂度 | 基础设施 | 低 | Docker 一键部署；开发阶段可先用本地文件系统替代，生产再切换 MinIO |

---

## 8. 开发优先级与里程碑规划

| 阶段 | 时间 | 重点交付物 | 核心功能 |
|:---|:---|:---|:---|
| **阶段一：基础架构** | 第 1-2 周 | 前后端项目脚手架、MySQL/MongoDB/MinIO 环境搭建、数据库建表、用户认证、Knife4j API 文档 | 用户系统、基础数据录入 |
| **阶段二：核心闭环 (P0)** | 第 3-4 周 | 推荐、查询、导航（Dijkstra/TSP）、日记 CRUD、地图基础展示 | **M1 交付：核心功能可演示** |
| **阶段三：功能扩展 (P1-A)** | 第 5-6 周 | 室内导航、拥挤度感知、协同规划、日记全文检索 | **M2 交付：增强功能可演示** |
| **阶段四：打磨完善 (P1-B)** | 第 7-8 周 | 自动生成日记、消费账单、AR 打卡、UI 打磨、集成测试 | **M3 交付：全部功能完整演示** |

### 开发模式

- **项目整体**：瀑布流模式，按上述 4 阶段推进
- **代码管理**：Git 功能分支工作流（feature/* / fix/* / release/*）
- **团队协作**：定期线下开会 + 飞书文档共享
- **接口规范**：后端通过 Knife4j 维护实时 API 文档，前端据此联调

---

## 附录 A：术语表

| 术语 | 定义 |
|------|------|
| MVP | Minimum Viable Product，最小可行产品 |
| TSP | Traveling Salesman Problem，旅行商问题 |
| Top-K | 从 N 个元素中取出最优的 K 个，无需全量排序 |
| 策略模式 | 设计模式，允许在运行时切换算法/行为实现 |
| 拥挤度 | 景区内某区域/道路的游客密集程度指标 |
| 反向游览 | 引导游客避开拥堵区域，优先游览人少区域 |
| JWT | JSON Web Token，无状态认证方案 |
| DTO | Data Transfer Object，数据传输对象 |

## 附录 B：页面结构总览

系统采用 4 个一级页面：

| 页面 | 前端路由 | 核心承载功能 | 主要组件 |
|------|---------|------------|---------|
| **主页** | `/home` | 搜索、推荐（景点/学校/美食/日记）、导航入口 | 搜索框、推荐卡片列表、分类 Tab、快捷功能入口 |
| **行程** | `/trip` | 路线规划（单目标/多目标/多策略）、路径回顾、附近设施 | 地图容器、路径规划表单、策略选择器、行程列表 |
| **日记** | `/diary` | 日记浏览、发布、评分、推荐、检索 | 日记卡片/瀑布流、发布编辑器、评分组件、搜索栏 |
| **个人中心** | `/profile` | 我的行程、收藏、历史记录、消费账单、偏好设置 | 设置表单、数据列表、统计图表 |

## 附录 C：核心数据表概览

### MySQL 核心表

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `user` | 用户基本信息 | id, username, password_hash, avatar_url |
| `user_preference` | 用户偏好 | id, user_id, preference_data (JSON) |
| `attraction` | 景点/学校 | id, name, category, description, rating, popularity |
| `building` | 建筑物 | id, attraction_id, name, type |
| `facility` | 服务设施 | id, attraction_id, name, type (enum), location |
| `node` | 图顶点 | id, attraction_id, building_id (nullable), floor (nullable), room (nullable), x, y |
| `edge` | 图边 | id, from_node_id, to_node_id, weight_distance, weight_time, weight_congestion, transport_type (enum) |
| `itinerary` | 行程 | id, user_id, name, created_at |
| `itinerary_node` | 行程节点 | id, itinerary_id, node_id, sequence, visited_at |
| `favorite` | 收藏（多态） | id, user_id, target_type (enum), target_id |
| `user_history` | 历史记录 | id, user_id, action_type, target_id, created_at |
| `diary_index` | 日记摘要索引（桥接表） | id, user_id, mongodb_id, title, destination, content_type, created_at |
| `expense_bill` | 消费账单 | id, user_id, total_amount, category_summary (JSON) |
| `service_link` | 外部服务链接 | id, attraction_id, link_type (enum), url |
| `photo_spot` | 拍照点位 | id, attraction_id, name, description, x, y |

### MongoDB 核心集合

| 集合名 | 用途 | 关键字段 |
|--------|------|---------|
| `diary` | 旅游日记 | user_id, title, content (rich text), media_refs[], tags[], rating_avg, view_count, content_type, path_refs[] |
| `comment` | 评论/点评 | user_id, target_type, target_id, content, rating, created_at |
| `diary_interaction` | 日记互动 | user_id, diary_id, action (view/like), created_at |

---

*本文档由 PM 智能体基于「需求分析-功能定义」「需求分析-功能呈现」「技术栈和开发模式协作方式」三份原始文档整合生成。*

*JourneyCraft - 智能旅游推荐系统* 📋
