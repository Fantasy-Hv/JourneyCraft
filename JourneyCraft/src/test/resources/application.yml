spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:journeycraft;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password: ''
  mongodb:
    uri: mongodb://localhost:27017/journeycraft_test

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: INFO
